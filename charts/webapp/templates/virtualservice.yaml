---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: webapp
  namespace: deployment
spec:
  gateways:
  - webapp-gateway
  hosts:
  - "webapp.example.com"
  http:
  - match:
    - port: 5001
      uri:
        prefix: /
    route:
    - destination:
        host: deployment-webapp-webapp
        port:
          number: 5001
