---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: webapp-vs
  namespace: deployment
spec:
  gateways:
  - webapp-gwy
  hosts:
  - "webapp.example.com"
  http:
  - match:
    - uri:
        exact: /
    - uri:
        exact: /buy
    - uri:
        exact: /healthcheck
    route:
    - destination:
        host: deployment-webapp-webapp
        port:
          number: 5001
