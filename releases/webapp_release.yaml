---
apiVersion: helm.integrations.flux.weave.works/v1alpha2
kind: FluxHelmRelease
metadata:
  name: webapp
  namespace: deployment
  labels:
    chart: webapp
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.webapp: semver:*.*

spec:
  chartGitPath: webapp
  values:
    webapp:
      replicas: 3
      image: registry.hub.docker.com/lmctest/test:0.6
      persistence:
        enabled: false
